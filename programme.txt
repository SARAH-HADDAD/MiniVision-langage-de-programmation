import numpy as np
import matplotlib.pyplot as plt
echiquierarray = ([200, 200]) for x in range(200):
for y in range(200):
if (x % 50) // 25 == (y % 50) // 25:
echiquierarray[x, y] = 0
else:
echiquierarray[x, y] = 255