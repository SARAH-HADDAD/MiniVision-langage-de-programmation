import numpy as Np
import matplotlib.pyplot as Plt
Array = ([200, 200]) for X in range(200):
for Y in range(200):
if (X % 50) // 25 == (y % 50) // 25:
Array[X, Y] = 0
else:
Array[X, Y] = 255
# ceci est un commentaire
